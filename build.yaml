version: 0.1

phases:
  install:
    commands:
      - yum install zip -y
  build:
    commands:
      - echo `pwd`
      - mkdir build
  post_build:
    commands:
      - echo Post-Build completed on `date`
      #Zip File
      - ls -R *
      - cd build
      - zip -r lambda.zip helloworld.py
      #Copy to S3
      #- aws s3 cp lambda.zip s3://web-dev-lambda-codebuild-s3/
artifacts:
  files:
    - 'build/*'
  name: lambda.zip
